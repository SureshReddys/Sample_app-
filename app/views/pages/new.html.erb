<%= link_to("<<Back to list",{:action=> :index,:subject_id => @subject.id}, :class=> 'action index') %>
  
   <div class="Pages new">
   	 <h2>New Page </h2>

   	  <%= form_for :page, url: { action: "create",:subject_id => @subject.id } do |f| %>

          <%= render(:partial => "form" , :locals => {:f => f}) %>
          
           <div class="form-buttons">
 	    	   <%= submit_tag("create pages") %>
 	        </div>

        <% end %>
</div>